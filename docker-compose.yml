version: "3"

services:
  jenkins:
    build:
      context: jenkins
    ports:
      - 8080:8080
      - 50000:50000
    volumes:
          - "jenkins-data:/var/jenkins_home"

  travis:
    image: gitlab/gitlab-ce
    ports:
      - 8081:80

volumes:
  jenkins-data:
